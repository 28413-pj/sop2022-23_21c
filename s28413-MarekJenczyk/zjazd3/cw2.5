#!/bin/bash

plik=("$@")
liczba_plik=${#files[@]}
for (( i=liczba_plik-1; i>=0; i-- )); do
    echo "Zawartość pliku ${plik[i]}:"
    liczba_plik=$(wc -l < "${plik[i]}")
    for (( j=liczba_plik; j>=1; j-- )); do
        sed -n "${j}p" "${plik[i]}"
    done
    echo ""
done
